# Kurasi.ai - Prompt 5: TRUST & TRANSPARENCY (FINAL!)

## 🎯 Copy to Replit - This is about TRUST!

-----

## THE TRUST PRINCIPLE

> “Untuk pemimpin yang menggerakkan pasar dan kebijakan, SATU informasi salah bisa berakibat fatal. Setiap berita HARUS bisa diverifikasi.”

-----

## Part A: Enhanced Brief Card with Full Transparency

```tsx
// components/BriefCard.tsx - TRUST VERSION

import { 
  ExternalLink, 
  Bookmark, 
  ThumbsDown, 
  ChevronDown, 
  ChevronUp,
  Shield,
  Link,
  Bot,
  AlertCircle,
  CheckCircle2
} from 'lucide-react';

interface BriefItem {
  title: string;
  summary: string;
  source: string;
  sourceType: 'local' | 'regional' | 'global';
  url: string;
  isPaywalled: boolean;
  whyItMatters: string;
  foundByModels: string[];
  verificationScore: number;
  publishedDate?: string;
}

function BriefCard({ item, priority }: { item: BriefItem; priority: string }) {
  const [expanded, setExpanded] = useState(priority === 'critical');
  
  const priorityStyles = {
    critical: 'border-l-red-500 bg-red-50/50',
    important: 'border-l-amber-500 bg-amber-50/50',
    background: 'border-l-green-500 bg-green-50/50'
  };
  
  // Trust score indicator
  const getTrustLevel = (score: number, modelCount: number) => {
    if (score >= 8 && modelCount >= 2) return { level: 'high', label: 'Sangat Terpercaya', color: 'green' };
    if (score >= 6 && modelCount >= 1) return { level: 'medium', label: 'Terpercaya', color: 'amber' };
    return { level: 'low', label: 'Perlu Verifikasi', color: 'red' };
  };
  
  const trust = getTrustLevel(item.verificationScore || 7, item.foundByModels?.length || 1);
  
  return (
    <div className={`bg-white rounded-xl border border-slate-200 border-l-4 ${priorityStyles[priority]} overflow-hidden`}>
      
      {/* Header */}
      <div className="p-4 cursor-pointer" onClick={() => setExpanded(!expanded)}>
        <div className="flex items-start justify-between gap-3">
          <div className="flex-1">
            <h3 className="font-semibold text-slate-900 leading-snug">{item.title}</h3>
            
            {/* Trust & Source Badges - ALWAYS VISIBLE */}
            <div className="flex items-center gap-2 mt-3 flex-wrap">
              
              {/* Source Badge with Flag */}
              <a 
                href={item.url} 
                target="_blank" 
                rel="noopener noreferrer"
                onClick={(e) => e.stopPropagation()}
                className="inline-flex items-center gap-1.5 text-xs bg-slate-100 hover:bg-slate-200 text-slate-700 px-2.5 py-1.5 rounded-lg transition-colors"
              >
                {item.sourceType === 'local' && <span>🇮🇩</span>}
                {item.sourceType === 'regional' && <span>🌏</span>}
                {item.sourceType === 'global' && <span>🌍</span>}
                <span className="font-medium">{item.source}</span>
                <ExternalLink className="w-3 h-3 opacity-50" />
              </a>
              
              {/* Paywall Badge */}
              {item.isPaywalled && (
                <span className="inline-flex items-center gap-1 text-xs bg-amber-100 text-amber-700 px-2 py-1.5 rounded-lg">
                  🔒 Berbayar
                </span>
              )}
              
              {/* Trust Badge */}
              <span className={`inline-flex items-center gap-1 text-xs px-2 py-1.5 rounded-lg ${
                trust.color === 'green' ? 'bg-green-100 text-green-700' :
                trust.color === 'amber' ? 'bg-amber-100 text-amber-700' :
                'bg-red-100 text-red-700'
              }`}>
                {trust.color === 'green' && <CheckCircle2 className="w-3 h-3" />}
                {trust.color === 'amber' && <Shield className="w-3 h-3" />}
                {trust.color === 'red' && <AlertCircle className="w-3 h-3" />}
                {trust.label}
              </span>
              
              {/* AI Agreement Badge */}
              {item.foundByModels?.length > 1 && (
                <span className="inline-flex items-center gap-1 text-xs bg-blue-100 text-blue-700 px-2 py-1.5 rounded-lg">
                  <Bot className="w-3 h-3" />
                  {item.foundByModels.length} AI setuju
                </span>
              )}
            </div>
          </div>
          
          {expanded ? 
            <ChevronUp className="w-5 h-5 text-slate-400 flex-shrink-0" /> : 
            <ChevronDown className="w-5 h-5 text-slate-400 flex-shrink-0" />
          }
        </div>
        
        {/* Preview when collapsed */}
        {!expanded && (
          <p className="text-slate-600 text-sm mt-3 line-clamp-2">{item.summary}</p>
        )}
      </div>
      
      {/* Expanded Content */}
      {expanded && (
        <div className="px-4 pb-4 border-t border-slate-100">
          
          {/* Full Summary */}
          <p className="text-slate-700 mt-4 leading-relaxed">{item.summary}</p>
          
          {/* Why It Matters - Personalized */}
          <div className="mt-4 p-4 bg-blue-50 rounded-xl border border-blue-100">
            <p className="text-sm font-semibold text-blue-800 mb-1">
              💡 Mengapa ini penting untuk Anda:
            </p>
            <p className="text-sm text-blue-700 leading-relaxed">
              {item.whyItMatters}
            </p>
          </div>
          
          {/* === TRUST SECTION === */}
          <div className="mt-4 p-4 bg-slate-50 rounded-xl border border-slate-200">
            <p className="text-xs font-semibold text-slate-500 uppercase tracking-wide mb-3">
              🔍 Transparansi & Verifikasi
            </p>
            
            <div className="space-y-2">
              {/* Source URL */}
              <div className="flex items-start gap-2">
                <Link className="w-4 h-4 text-slate-400 mt-0.5 flex-shrink-0" />
                <div className="flex-1 min-w-0">
                  <p className="text-xs text-slate-500">Sumber Asli:</p>
                  {item.url && item.url !== 'perlu verifikasi' ? (
                    <a 
                      href={item.url}
                      target="_blank"
                      rel="noopener noreferrer"
                      className="text-sm text-blue-600 hover:text-blue-800 hover:underline break-all"
                    >
                      {item.url}
                    </a>
                  ) : (
                    <p className="text-sm text-slate-400 italic">URL perlu verifikasi manual</p>
                  )}
                </div>
              </div>
              
              {/* AI Models that found this */}
              <div className="flex items-start gap-2">
                <Bot className="w-4 h-4 text-slate-400 mt-0.5 flex-shrink-0" />
                <div>
                  <p className="text-xs text-slate-500">Ditemukan oleh:</p>
                  <div className="flex flex-wrap gap-1.5 mt-1">
                    {(item.foundByModels || ['AI']).map((model, idx) => (
                      <span 
                        key={idx}
                        className="inline-flex items-center text-xs bg-white border border-slate-200 text-slate-600 px-2 py-1 rounded"
                      >
                        {model}
                      </span>
                    ))}
                  </div>
                </div>
              </div>
              
              {/* Verification Score */}
              <div className="flex items-start gap-2">
                <Shield className="w-4 h-4 text-slate-400 mt-0.5 flex-shrink-0" />
                <div>
                  <p className="text-xs text-slate-500">Skor Verifikasi:</p>
                  <div className="flex items-center gap-2 mt-1">
                    <div className="flex-1 h-2 bg-slate-200 rounded-full max-w-[100px]">
                      <div 
                        className={`h-2 rounded-full ${
                          (item.verificationScore || 7) >= 8 ? 'bg-green-500' :
                          (item.verificationScore || 7) >= 6 ? 'bg-amber-500' : 'bg-red-500'
                        }`}
                        style={{ width: `${(item.verificationScore || 7) * 10}%` }}
                      />
                    </div>
                    <span className="text-sm font-medium text-slate-700">
                      {item.verificationScore || 7}/10
                    </span>
                  </div>
                </div>
              </div>
              
              {/* Published Date if available */}
              {item.publishedDate && (
                <div className="flex items-center gap-2 text-xs text-slate-500">
                  <span>📅 Dipublikasikan: {item.publishedDate}</span>
                </div>
              )}
            </div>
          </div>
          
          {/* Paywall Notice */}
          {item.isPaywalled && (
            <div className="mt-3 p-3 bg-amber-50 rounded-lg border border-amber-200">
              <p className="text-xs text-amber-700">
                🔒 Artikel lengkap memerlukan langganan {item.source}. 
                Kami hanya menampilkan ringkasan dari metadata publik.
              </p>
            </div>
          )}
          
          {/* Actions */}
          <div className="flex items-center justify-between mt-4 pt-4 border-t border-slate-100">
            {item.url && item.url !== 'perlu verifikasi' && (
              <a
                href={item.url}
                target="_blank"
                rel="noopener noreferrer"
                className="inline-flex items-center gap-1.5 text-blue-600 hover:text-blue-800 text-sm font-medium"
              >
                Buka Sumber Asli <ExternalLink className="w-4 h-4" />
              </a>
            )}
            
            <div className="flex items-center gap-1">
              <button 
                className="p-2 text-slate-400 hover:text-amber-600 hover:bg-amber-50 rounded-lg transition-colors"
                title="Simpan"
              >
                <Bookmark className="w-4 h-4" />
              </button>
              <button 
                className="p-2 text-slate-400 hover:text-red-600 hover:bg-red-50 rounded-lg transition-colors"
                title="Tidak relevan / Salah"
              >
                <ThumbsDown className="w-4 h-4" />
              </button>
            </div>
          </div>
          
        </div>
      )}
    </div>
  );
}

export default BriefCard;
```

-----

## Part B: Council Transparency Footer

Add this at the bottom of every brief:

```tsx
// components/CouncilFooter.tsx

function CouncilFooter({ councilResults }: { councilResults?: any }) {
  return (
    <div className="bg-slate-900 rounded-xl p-6 text-white mt-8">
      <div className="flex items-start gap-4">
        <div className="w-10 h-10 bg-amber-500 rounded-xl flex items-center justify-center flex-shrink-0">
          <Shield className="w-5 h-5 text-white" />
        </div>
        <div>
          <h3 className="font-semibold mb-1">Komitmen Transparansi Kurasi.ai</h3>
          <p className="text-slate-300 text-sm leading-relaxed">
            Brief ini dikurasi oleh Dewan AI yang bekerja independen. 
            Setiap berita dapat diverifikasi ke sumber aslinya. 
            Kami tidak mengedit atau mengubah fakta dari sumber.
          </p>
          
          <div className="mt-4 pt-4 border-t border-slate-700">
            <p className="text-xs text-slate-400 mb-2">MODEL AI YANG DIGUNAKAN:</p>
            <div className="flex flex-wrap gap-2">
              <span className="text-xs bg-slate-800 px-2 py-1 rounded">GPT-4o (OpenAI)</span>
              <span className="text-xs bg-slate-800 px-2 py-1 rounded">DeepSeek V3</span>
              <span className="text-xs bg-slate-800 px-2 py-1 rounded text-slate-500">+ Claude, Gemini saat aktif</span>
            </div>
          </div>
          
          <div className="mt-3">
            <p className="text-xs text-slate-400">
              ⚠️ AI dapat membuat kesalahan. Selalu verifikasi informasi penting sebelum mengambil keputusan.
            </p>
          </div>
        </div>
      </div>
    </div>
  );
}
```

-----

## Part C: Update LLM Prompt for Trust

Add verification requirements to the search prompts:

```typescript
const TRUST_REQUIREMENTS = `
ATURAN VERIFIKASI (WAJIB!):

1. HANYA sertakan berita yang Anda YAKIN benar
   - Jika ragu, JANGAN sertakan
   - Lebih baik 3 berita valid daripada 5 berita meragukan

2. SETIAP artikel HARUS punya:
   - URL yang valid (bukan karangan)
   - Sumber yang bisa diverifikasi
   - Tanggal publikasi

3. SKOR VERIFIKASI (1-10):
   - 9-10: Dari sumber resmi (BI, OJK, pemerintah, perusahaan langsung)
   - 7-8: Dari media terpercaya (Kontan, Bisnis Indonesia, Reuters, Bloomberg)
   - 5-6: Dari media umum (Kompas, Detik)
   - 1-4: Dari sumber tidak terverifikasi (JANGAN GUNAKAN)

4. ANTI MISINFORMASI:
   - Jangan sertakan berita dari sumber yang dikenal menyebar hoax
   - Jangan sertakan opini sebagai fakta
   - Jangan sertakan rumor tanpa konfirmasi
   - Jika berita kontroversial, sertakan multiple sources

5. PAYWALL:
   - Jika sumber berbayar, set isPaywalled: true
   - Tetap sertakan jika relevan - user mungkin punya akses
   - Jangan coba bypass paywall

INGAT: User kita adalah pemimpin yang keputusannya berdampak besar.
SATU informasi salah bisa berakibat fatal. BE RESPONSIBLE.
`;

// Add to the search prompt
function createSearchPrompt(profile: UserProfile, modelName: string): string {
  return `${profile.council_system_prompt}

${INDONESIAN_FIRST_SEARCH_PROMPT}

${TRUST_REQUIREMENTS}

... rest of prompt ...`;
}
```

-----

## Part D: Disclaimer in Email (Future)

```html
<!-- Add to email footer -->
<div style="background: #f8fafc; padding: 20px; border-top: 1px solid #e2e8f0; font-size: 12px; color: #64748b;">
  <p style="margin: 0 0 10px 0;">
    <strong>⚠️ Disclaimer:</strong> Brief ini dikurasi oleh AI dan mungkin mengandung kesalahan. 
    Selalu verifikasi informasi penting ke sumber asli sebelum mengambil keputusan.
  </p>
  <p style="margin: 0;">
    Setiap berita dilengkapi link ke sumber asli untuk verifikasi Anda.
  </p>
</div>
```

-----

## Part E: Trust Metrics Dashboard (Simple)

```tsx
// Add to Dashboard header when brief is loaded

{brief && (
  <div className="grid grid-cols-3 gap-3 mb-6">
    <div className="bg-white rounded-xl border border-slate-200 p-4 text-center">
      <p className="text-2xl font-bold text-green-600">
        {brief.critical?.filter(i => i.verificationScore >= 8).length + 
         brief.important?.filter(i => i.verificationScore >= 8).length || 0}
      </p>
      <p className="text-xs text-slate-500 mt-1">Sangat Terpercaya</p>
    </div>
    <div className="bg-white rounded-xl border border-slate-200 p-4 text-center">
      <p className="text-2xl font-bold text-blue-600">
        {[...brief.critical || [], ...brief.important || [], ...brief.background || []]
          .filter(i => i.sourceType === 'local').length}
      </p>
      <p className="text-xs text-slate-500 mt-1">Sumber Indonesia 🇮🇩</p>
    </div>
    <div className="bg-white rounded-xl border border-slate-200 p-4 text-center">
      <p className="text-2xl font-bold text-slate-600">
        {new Set([...brief.critical || [], ...brief.important || [], ...brief.background || []]
          .flatMap(i => i.foundByModels || [])).size}
      </p>
      <p className="text-xs text-slate-500 mt-1">Model AI Aktif</p>
    </div>
  </div>
)}
```

-----

## TESTING CHECKLIST ✅

After applying Prompt 5, verify:

```
[ ] Each brief item shows:
    [ ] Source name with flag (🇮🇩/🌏/🌍)
    [ ] Clickable source URL
    [ ] Trust badge (Sangat Terpercaya/Terpercaya/Perlu Verifikasi)
    [ ] "X AI setuju" badge if multiple models found it
    [ ] Verification score bar (X/10)
    [ ] Which AI models found the article

[ ] Council Footer shows:
    [ ] Transparency commitment
    [ ] List of AI models used
    [ ] Disclaimer about AI limitations

[ ] Trust metrics in dashboard header
```

-----

## WHAT YOU NEED TO PROVIDE:

### 1. API Keys (Add to Replit Secrets):

```
DEEPSEEK_API_KEY=sk-xxx     ← You have this ✅
OPENAI_API_KEY=sk-xxx       ← You have this ✅
```

### 2. Test the Flow:

```
1. Open the app URL
2. Try demo chat on landing page
3. Register a new account
4. Complete onboarding conversation
5. Click "Buat Brief" 
6. Verify trust indicators appear
7. Click source URLs - they should open
```

### 3. Share the URL:

Once tested, share with me and I’ll help verify!

-----

## BISMILLAH - READY TO SHIP! 🚀🇮🇩

Copy Prompt 5 to Replit, add your API keys, and TEST!

Let me know the app URL when ready - I’ll help you verify everything works!